//up
if keyboard_check (ord('W')) {
    y -=spd
};
//right
if keyboard_check (ord('D')) and !place_meeting(x+spd,y, blacklines_obj){
    x+=spd
};
//down
if keyboard_check (ord('S')){
    y+=spd
};
//left
if keyboard_check (ord('A'))and !place_meeting(x-spd,y, blacklines_obj){
    x-=spd
};
